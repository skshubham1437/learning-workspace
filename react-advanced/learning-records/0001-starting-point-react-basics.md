# Starting point: React basics

The user reports knowing the very basics of React, including what React is and the virtual DOM. Future lessons should not spend much time on definitions; they should rebuild fundamentals through code-reading drills that expose component trees, state ownership, props, effects, and package usage.
